#set("title") {Welcome}

#set("body") {
    <h1>All projects</h1>
    <ul>
    #for(project in projects) {
        <li class="lead">
            <strong>#(project.name)</strong><br />
            #(project.description)<br />
            <span class="badge alert-info">#(project.language)</span></a>
        </li>
    }
    </ul>

    <p class="lead"><a href="/projects/new">Create a project</a>.</p>
}

#embed("master")